services:
  aioabs-backbone:
    build: .
    container_name: aio-backbone
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./src/config:/src/config
      - ./logs:/logs
    restart: unless-stopped
